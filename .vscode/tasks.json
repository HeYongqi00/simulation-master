{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
		{
			"label": "colcon make",
			"detail": "Build workspace (default)",
			"type": "shell",
			"command": "colcon build --packages-select tuning_mpc",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": "$gcc"
		},
	]
}